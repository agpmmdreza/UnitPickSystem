import {ReactComponent as CaptureSvg} from "assets/icons/bulk/capture.svg";

function CaptureIcon(props: { [key: string]: any }) {
  return <CaptureSvg data-variant="bulk" {...props} />;
}

export { CaptureIcon };
