import {ReactComponent as ProfileConsentSVG} from "assets/icons/bulk/profile-consent.svg";

function ProfileConsentIcon(props: { [key: string]: any }) {
  return <ProfileConsentSVG data-variant="bulk" {...props} />;
}

export { ProfileConsentIcon };
